apiVersion: v1
kind: Service
metadata:
  name: {{ include "inferflux.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "inferflux.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
